describe("Sample Test with it()", () => {
  it("should sum numbers correctly", () => {
    const result = 1 + 2;
    expect(result).toBe(3);
  });

  it("should subtract numbers correctly", () => {
    const result = 5 - 2;
    expect(result).toBe(3);
  });
});
